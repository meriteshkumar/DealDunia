@model IEnumerable<DealDunia.Domain.Entities.OfferURL>

@if (Model.Count() > 0)
{
    <div class="row">
        <h4>OFFERS / LINKS</h4>
        <hr />
        @foreach (var offerURL in Model)
        {   
            <div class="divDeal">
                <div class="divDealTitle">
                    <b><a target="_blank" href="@offerURL.offer_url">@offerURL.name</a></b>
                </div>
                <div class="divDealCodeOuter">
                    <span><a style="color:white;" target="_blank" href="@offerURL.offer_url">VISIT OFFER</a></span>
                    <br />
                </div>
            </div>
        }
    </div>
}

@*@if (Model.Count() > 0)
{
    <div class="row">
        <h4>OFFERS / LINKS</h4>
        <hr />
        @foreach (var offerURL in Model)
        {   
            <div class="panel panel-info" style="float: left; width: 240px; height: auto; font-family: Verdana; margin: 5px; padding: 5px; text-align: center; vertical-align: middle">
                <div style="font-size: 12px; padding: 5px;">
                    <b><a target="_blank" href="@offerURL.offer_url">@offerURL.name</a></b>
                </div>
                <div style="padding: 10px;">
                    <div style="margin: auto; height: 30px; width: 140px; background-color: #bf192a; font-weight: bold; color: gray; font-size: 11px; padding: 5px; border: 1px #fff dashed;">
                        <span><a style="color:white;" target="_blank" href="@offerURL.offer_url">VISIT OFFER</a></span>
                        <br />
                    </div>
                </div>
            </div>
        }
    </div>
}*@